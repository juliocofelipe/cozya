import type { ReactNode } from "react";

export const metadata = {
  title: "Cozya – Entrar",
  description: "Acesse sua conta para salvar e organizar receitas rápidas"
};

export default function AuthLayout({ children }: { children: ReactNode }) {
  return children;
}
